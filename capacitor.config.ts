import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snakegame.bilal',
  appName: 'snake-game',
  webDir: 'dist/browser',
};

export default config;
